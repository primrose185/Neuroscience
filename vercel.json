{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git lfs install && (git remote set-url origin https://github.com/primrose185/Neuroscience.git 2>/dev/null || git remote add origin https://github.com/primrose185/Neuroscience.git) && git lfs pull && git lfs ls-files -s && echo 'lfs pull done' && yarn build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/assets/(.*)",
      "dest": "/assets/$1"
    },
    {
      "src": "/(.*\\.(js|css|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot))",
      "dest": "/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 